#include <stdio.h>

int main()
{
    // This is a single line comment. (VS Code shortcut --> CTRL + /)

    /* This is a
    multiline comment. (VS Code shortcut --> SHIFT + ALT + A)
    */
    return 0;
}