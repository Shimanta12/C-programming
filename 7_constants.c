#include <stdio.h>

int main()
{
    /*
     If we dont want anyone or ourselves change the value of variable, we can use the const keyword
     This will declare the variable as constant, which means unchangable and read only.
     */

    const int PI = 3.1416; // When we declare a constant variable, it must be assigned with a value in the time of declaration

    return 0;
}