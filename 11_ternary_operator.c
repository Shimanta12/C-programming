#include <stdio.h>

int main()
{
    // Short hand if else.
    char letter = 'a';
    letter == 'a' || letter == 'e' || letter == 'i' || letter == 'o' || letter == 'u' ? printf("Vowel.\n") : printf("Consonent.\n");
    return 0;
}